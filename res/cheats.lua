local cheats = {
  modfly = {
    hint = "",
    enable = function()
      return autoassemble([[]])
    end
  },
  antibounce = {
    hint = "",
    enable = function()
      return autoassemble([[]])
    end
  }
}
return cheats
